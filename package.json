{
  "name": "cem-connect",
  "version": "0.1.0",
  "private": true,
  "description": "MVP visual do CEM CONNECT",
  "scripts": {
    "dev": "node server.mjs",
    "start": "node server.mjs",
    "create-admin": "node scripts/create-admin.mjs",
    "build": "node scripts/check.mjs && node --check server.mjs && node --check src/server/database.mjs && node --check src/app.js && node --check scripts/create-admin.mjs"
  },
  "engines": {
    "node": ">=22.5"
  },
  "dependencies": {
    "qrcode": "^1.5.4"
  }
}
