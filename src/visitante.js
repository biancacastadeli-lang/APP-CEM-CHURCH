const form=document.querySelector('#visitor-form');const message=document.querySelector('#message');const phone=form.whatsapp;
phone.addEventListener('input',()=>{const n=phone.value.replace(/\D/g,'').slice(0,11);phone.value=n.length>10?`(${n.slice(0,2)}) ${n.slice(2,7)}-${n.slice(7)}`:`(${n.slice(0,2)}) ${n.slice(2,6)}-${n.slice(6)}`});
form.addEventListener('submit',async(event)=>{event.preventDefault();message.textContent='';const data={name:form.name.value.trim(),whatsapp:phone.value,consent:form.consent.checked};try{const r=await fetch('/api/public/visitors',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(data)});if(!r.ok)throw Error((await r.json()).error);form.hidden=true;message.textContent='Recebemos suas informações. Obrigado por estar conosco!';message.className='success'}catch(error){message.textContent=error.message||'Não foi possível enviar agora. Tente novamente.';message.className='error'}});
